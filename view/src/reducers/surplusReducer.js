const initialState = {
  surpluses: [],
  surplus: {},
  loading: false,
};

export default function SurplusReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
